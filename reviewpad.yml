workflows:
  - name: ping
    run:
      - $addLabel("ping")

          
checks:
  large:
    parameters:
      max-lines: 4
    severity: high
  stale:
    parameters:
      max-idle-days: 0
  long-lived:
    parameters:
      max-days: 0  


