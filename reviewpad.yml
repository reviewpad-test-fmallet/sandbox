
workflows:
  - name: For Test Purpose
    run:
      - if: true == true
        then: $failCheckStatus($sprintf("failure %s", ["1"]))
      - if: true == true
        then: $failCheckStatus("failure 2")
