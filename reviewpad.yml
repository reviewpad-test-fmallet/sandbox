workflows:
  - name: foo
    run:
      - $addLabel("foo")

checks:
  gitConflicts:
  largePR:
    parameters:
      maxLinesOfCode: 1
  longPickUpTime:
    parameters:
      hours: 0
  longLived:
    parameters: 
      maxDays: 0

  


