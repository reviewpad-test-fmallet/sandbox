workflows:
  - name: blacklist-binary-files
    run:
      if: '$any(["file", "folder/file"], ($file: String => $isBinaryFileFile($file)))'
      then: $fail("Blacklisted binary files aren't allowed in pull request")
